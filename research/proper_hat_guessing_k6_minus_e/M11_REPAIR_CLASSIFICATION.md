# Classification of the M11 deletion repairs

Let `G` be the explicitly generated sharply four-transitive group on

\[
\Omega=\{0,1,\ldots,10\}
\]

with base ordered tuple `(0,1,2,3)`. For every label `y` outside the base tuple, define

\[
F_y(g(0),g(1),g(2),g(3))=g(y).
\]

Delete point `10` and apply the coordinate-replacement repair described in the main theorem.

## Proposition

The seven labels split as follows.

```text
coherently repairable: 4,5,6,7,8,10
not repairable:        9
```

For every repairable label:

```text
tuples whose old output is 10:       720
replacement count for each colour:    72
coordinate lines:                   2,880
cycle type on every line:            (3,4)
```

Among the fifteen unordered pairs of repairable functions, exactly three satisfy the twin-completion line criterion:

\[
\boxed{\{4,6\},\quad\{5,10\},\quad\{7,8\}.}
\]

For each compatible pair the two repaired functions are pointwise unequal on all 5,040 tuples and the linewise composition is a 7-cycle on all 2,880 lines. Every incompatible pair fails the composition condition on exactly 720 lines.

## Stabilizer explanation

The setwise stabilizer of the base tetrad has order 24 and has two orbits on the seven remaining labels:

\[
\{9\},\qquad \{4,5,6,7,8,10\}.
\]

The orbit of the five-set

\[
\{0,1,2,3,9\}
\]

contains 66 blocks, and every four-subset of the eleven-point set lies in exactly one of them. Thus this orbit is an `S(4,5,11)` Witt design, and `9` is the distinguished Witt completion of the base tetrad. It is exactly the label whose deletion repair is inconsistent.

The three compatible pairs form one orbit under the tetrad stabilizer. Hence the chosen pair `{4,6}` is one of three symmetry-equivalent slots, rather than an isolated lucky choice.

All assertions above are regenerated by `code/verify_k6e_m11.py` from the two group generators; no classification theorem about `M11` is a premise.